# Вариант 6 — письменный экзамен

**Специальность:** «Проектирование, разработка и управление сложными информационными системами»  
**Время:** 180 минут  
**Задания 1–6** — на бумаге, **задания 7–8** — на компьютере (Python).

---

## Задание 1 (10 баллов)

$$\left(\frac{480\cdot 10^{-1}}{2}\right):\left(\frac{\left(29\cdot2+8:0.04\right):\frac{1}{5}}{\left(2:0.25-0.4:0.5\right):0.02}-1.5\right)^{-1}$$

---

## Задание 2 (10 баллов)

$$\sqrt{1936(x+1)^2-1}>\sqrt{1936-(x-1)^2}$$

---

## Задание 3 (15 баллов)

Найдите радиус вписанной окружности треугольника со сторонами 13, 14 и 15.

---

## Задание 4 (15 баллов)

Артём и Соня три раза обменивались наклеек. Каждый раз одна четвёртая часть наклеек Артёма менялась на одна треть наклеек Соняы. Сколько наклеек было изначально, если после второго обмена у Артёма осталось 720, а после третьего обмена у Соняы осталось 490?

---

## Задание 5 (10 баллов)

В выражении $358_{x54}+1x0_{100}$:

а) наибольшее $x$ при кратности 99; б) частное. *Ответ в десятичной системе.*

---

## Задание 6 (10 баллов)

Программа должна выводить **наибольшую разность двух соседних цифр (по модулю)**. Найдите ошибки:

```python
n = 63829174
mx = 0
while n >= 10:
    a = n % 10
    n = n // 10
    b = n % 10
    if abs(a - b) >= mx:
        mx = a - b
print(mx)
```

---

## Задание 7 (15 баллов)

Графики: y = x² − 3, y = 4 − x², y = 2 − x. Закрашенные области (см. `variant-6-blank.html`, рис. б).

| Ввод | Вывод |
|------|-------|
| -1.5 1.5 | YES |
| 1.5 1.5 | NO |

---

## Задание 8 (15 баллов)

Разработайте программу для исполнителя «Сириус», которая **методом Ньютона** находит приближённый корень уравнения

$$f(x)=x^{4} - 10 x^{2} + 9=0$$

с точностью $\varepsilon=0{,}001$.

**Вход:** $r1$ — начальное приближение (0,8); $r2$ — требуемая точность ε (0,001); $r3$ — максимальное число итераций (20).

**Выход:** приближённое значение корня в регистре $r5$.

**Требования:**

1. На каждой итерации храните в массиве памяти w[] значения x, f(x), f′(x) и новое приближение — не в стеке.
2. Обновляйте как минимум w[0]–w[3] на каждом шаге.
3. Остановка: |f(x)| < ε или достигнут лимит итераций r3.

$w[0]$ — текущее $x$, $w[1]$ — $f(x)$, $w[2]$ — $f'(x)$, $w[3]$ — новое $x$

### Команды регистров ($r0$–$r7$)

| Символ | Действие | Пример | Результат |
|--------|----------|--------|-----------|
| $m$ | присвоение | `m r0 2.5` | $r0 = 2{,}5$ |
| $+$ | сложение | `+ r0 r1` | $r0 = r0 + r1$ |
| $-$ | вычитание | `- r1 r2` | $r1 = r1 - r2$ |
| $*$ | умножение | `* r2 r0` | $r2 = r2 \cdot r0$ |
| $/$ | деление | `/ r3 r2` | $r3 = r3 / r2$ |
| $^$ | степень | `^ r4 r1` | $r4 = r4^{r1}$ |

### Команды массива памяти $w[0]$–$w[9]$

| Команда | Действие | Пример | Результат |
|---------|----------|--------|-----------|
| `mw` | запись | `mw 0 r1` | $w[0] = r1$ |
| `mr` | чтение | `mr r4 2` | $r4 = w[2]$ |
| `+w −w *w /w` | арифметика | `+w 0 r4` | $w[0] = w[0] + r4$ |

*Пример фрагмента (запись начального приближения в w[0]):*

```
mw 0 r1
```

*Вычисление $f(x)$ (вычисление f(x) при x = w[0]):*

```
mr r4 0
m r5 r4
m r7 4
^ r5 r7
m r0 r5
m r5 r4
m r7 2
^ r5 r7
m r7 10
* r5 r7
m r7 0
- r7 r5
m r5 r7
- r0 r5
m r5 9
+ r0 r5
mw 1 r0
```


---

## Ответы

| № | Ответ |
|---|-------|
| 1 | $50$ |
| 2 | $87$ |
| 3 | $4$ |
| 4 | Артём — $924$, Соня — $261$ |
| 5а | $7$ |
| 5б | $17374$ |
| 6 | `mx = 0` → `mx = -1`; `>=` → `>`; `mx = a - b` → `mx = abs(a - b)` |
| 7 | см. условия выше |
| 8 | $1$; замена t = x² → t² − 10t + 9 = 0 → t = 1 или 9 → x = ±1, ±3; программа: $\approx 1$ |
