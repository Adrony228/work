# Вариант 7 — письменный экзамен

**Специальность:** «Проектирование, разработка и управление сложными информационными системами»  
**Время:** 180 минут  
**Задания 1–6** — на бумаге, **задания 7–8** — на компьютере (Python).

---

## Задание 1 (10 баллов)

$$\left(\frac{600\cdot 10^{-1}}{2}\right):\left(\frac{\left(29\cdot2+8:0.04\right):\frac{1}{5}}{\left(2:0.2-0.8:0.2\right):0.02}-0.8\right)^{-1}$$

---

## Задание 2 (10 баллов)

$$\sqrt{2025(x+1)^2-1}>\sqrt{2025-(x-1)^2}$$

---

## Задание 3 (15 баллов)

В треугольнике $ABC$ угол $C$ равен $90^\circ$, $AC=9$, $BC=12$. Найдите радиус описанной окружности.

---

## Задание 4 (15 баллов)

Коля и Лена три раза обменивались значков. Каждый раз одна четвёртая часть значков Коляа менялась на одна треть значков Ленаы. Сколько значков было изначально, если после второго обмена у Коляа осталось 800, а после третьего обмена у Ленаы осталось 550?

---

## Задание 5 (10 баллов)

В выражении $247_{x63}+1x3_{100}$:

а) наибольшее $x$ при кратности 99; б) частное. *Ответ в десятичной системе.*

---

## Задание 6 (10 баллов)

Программа должна выводить **количество цифр, равных последней цифре числа**. Найдите ошибки:

```python
n = 52743525
last = n % 10
cnt = 0
while n:
    if n % 10 == last:
        cnt = cnt - 1
    n = n // 10
print(cnt)
```

---

## Задание 7 (15 баллов)

Графики: y = −x² + 1, y = 3 − x², y = x − 2. Закрашенная область (см. `variant-7-blank.html`, рис. б).

| Ввод | Вывод |
|------|-------|
| -1 1 | YES |
| 1.5 1.5 | NO |

- $y \ge -x^2 + 1$
- $y \le 3 - x^2$
- $y \ge x - 2$

---

## Задание 8 (15 баллов)

Разработайте программу для исполнителя «Сириус», которая **методом половинного деления** находит приближённый корень уравнения

$$f(x)=x^{4} - 3 x - 1=0$$

на отрезке $[1;\, 2]$ с точностью $\varepsilon=0{,}001$.

**Вход:** $r1$ — левая граница отрезка (1); $r2$ — правая граница отрезка (2); $r3$ — требуемая точность ε (0,001).

**Выход:** приближённое значение корня в регистре $r5$.

**Требования:**

1. Промежуточные значения (границы, середина, значения функции) храните в массиве памяти w[0], w[1], … — не в стеке.
2. На каждой итерации обновляйте w[0], w[1], w[2], w[3].
3. Итерации выполняйте, пока |w[1] − w[0]| > ε.

$w[0]$ — левая граница, $w[1]$ — правая, $w[2]$ — середина, $w[3]$ — $f$(середина)

### Команды регистров ($r0$–$r7$)

| Символ | Действие | Пример | Результат |
|--------|----------|--------|-----------|
| $m$ | присвоение | `m r0 2.5` | $r0 = 2{,}5$ |
| $+$ | сложение | `+ r0 r1` | $r0 = r0 + r1$ |
| $-$ | вычитание | `- r1 r2` | $r1 = r1 - r2$ |
| $*$ | умножение | `* r2 r0` | $r2 = r2 \cdot r0$ |
| $/$ | деление | `/ r3 r2` | $r3 = r3 / r2$ |
| $^$ | степень | `^ r4 r1` | $r4 = r4^{r1}$ |

### Команды массива памяти $w[0]$–$w[9]$

| Команда | Действие | Пример | Результат |
|---------|----------|--------|-----------|
| `mw` | запись | `mw 0 r1` | $w[0] = r1$ |
| `mr` | чтение | `mr r4 2` | $r4 = w[2]$ |
| `+w −w *w /w` | арифметика | `+w 0 r4` | $w[0] = w[0] + r4$ |

*Пример фрагмента (инициализация границ отрезка и вычисление середины):*

```
mw 0 r1
mw 1 r2
mr r0 0
+ r0 r1
mr r4 1
+ r0 r4
m r7 2
/ r0 r7
mw 2 r0
```

*Вычисление $f(x)$ (вычисление f(x) в середине отрезка (x = w[2])):*

```
mr r4 2
m r5 r4
m r7 4
^ r5 r7
m r0 r5
m r5 r4
m r7 3
* r5 r7
m r7 0
- r7 r5
m r5 r7
- r0 r5
m r5 0
m r7 1
- r5 r7
- r0 r5
mw 3 r0
```


---

## Ответы

| № | Ответ |
|---|-------|
| 1 | $105$ |
| 2 | $89$ |
| 3 | $7{,}5$ |
| 4 | Коля — $1004$, Лена — $321$ |
| 5а | $7$ |
| 5б | $11900$ |
| 6 | `cnt = cnt - 1` → `cnt = cnt + 1`; цикл должен сохранять `last` до изменения `n` (корректно); при отсутствии совпадений выводится отрицательное — логика верна после исправления знака |
| 7 | см. условия выше |
| 8 | $\approx 1,54$ |
