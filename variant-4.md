# Вариант 4 — письменный экзамен

**Специальность:** «Проектирование, разработка и управление сложными информационными системами»  
**Время:** 180 минут  
**Задания 1–6** — на бумаге, **задания 7–8** — на компьютере (Python).

---

## Задание 1 (10 баллов)

Найдите значение выражения:

$$\left(\frac{720\cdot 10^{-1}}{2}\right):\left(\frac{\left(20\cdot3+5:0.05\right):\frac{1}{5}}{\left(2:\frac{1}{5}-0.5:\frac{1}{4}\right):0.02}-1\right)^{-1}$$

---

## Задание 2 (10 баллов)

Найдите количество целых значений $x$, при которых выполняется неравенство:

$$\sqrt{2209(x+1)^2-1}>\sqrt{2209-(x-1)^2}$$

---

## Задание 3 (15 баллов)

Найдите площадь треугольника со сторонами 13, 14 и 15.

---

## Задание 4 (15 баллов)

Коля и Лена три раза обменивались значками. Каждый раз одна четвёртая часть значков Коли менялась на одну треть значков Лены. Сколько значков было у Коли и Лены изначально, если после второго обмена у Коли осталось 800 значков, а после третьего обмена у Лены осталось 550?

---

## Задание 5 (10 баллов)

В выражении $358_{x63}+1x9_{100}$ ($x$ — неизвестная цифра):

а) определите наибольшее значение $x$, при котором значение выражения кратно 99;

б) для найденного $x$ найдите частное от деления значения выражения на 99.

*Ответ запишите в десятичной системе счисления.*

---

## Задание 6 (10 баллов)

Саша написал программу, которая по натуральному числу $n$ должна выводить **наибольшую разность двух соседних цифр** этого числа (по модулю). В коде есть ошибки (рис. а).

а) Укажите ошибки.  
б) Запишите исправленные строки.

```python
n = 63829174
mx = 0
while n >= 10:
    a = n % 10
    n = n // 10
    b = n % 10
    if abs(a - b) >= mx:
        mx = a - b
print(mx)
```

---

## Задание 7 (15 баллов)

На координатной плоскости заданы графики функций $y=x^2-4$, $y=5-x^2$ и $y=3-2x$. Закрашенной отмечена область, ограниченная этими графиками (рис. б).

Напишите программу, которая по координатам точки $(x,y)$ определяет, принадлежит ли она закрашенной области.

**Формат ввода:** строка из двух вещественных чисел $x$ и $y$.  
**Формат вывода:** `YES`, если точка принадлежит области, иначе `NO`.

| Ввод | Вывод |
|------|-------|
| -1 2 | YES |
| 1.5 1.5 | NO |

**Описание закрашенной области** (для рисунка):

- $y \ge x^2 - 4$ (выше параболы $y=x^2-4$);
- $y \le 5 - x^2$ (ниже параболы $y=5-x^2$);
- $y \le 3 - 2x$ (ниже или на прямой $y=3-2x$).

---

## Задание 8 (15 баллов)

Разработайте программу для исполнителя «Сириус», которая **методом Ньютона** находит приближённый корень уравнения

$$f(x)=x^3-2x-5=0$$

с точностью $\varepsilon=0{,}001$.

**Вход:**

- $r1$ — начальное приближение $x_0$ ($2{,}5$);
- $r2$ — требуемая точность $\varepsilon$ ($0{,}001$);
- $r3$ — максимальное число итераций ($20$).

**Выход:** приближённое значение корня в регистре $r5$.

**Требования к решению:**

1. Промежуточные значения на каждой итерации храните в **массиве памяти** $w[0], w[1], \ldots$:
   - $w[0]$ — текущее приближение $x$;
   - $w[1]$ — значение $f(x)$;
   - $w[2]$ — значение $f'(x)=3x^2-2$;
   - $w[3]$ — новое приближение $x - f(x)/f'(x)$.
2. Использование **стека** для промежуточных значений **не допускается**.
3. Итерации выполняйте, пока $|w[1]| > \varepsilon$ и счётчик итераций не превысит $r3$.

### Регистры и массив памяти

Исполнитель имеет 8 регистров $r0$–$r7$ и **массив памяти** $w[0], w[1], \ldots, w[9]$.

### Команды работы с регистрами

| Символ | Действие | Пример | Результат |
|--------|----------|--------|-----------|
| $m$ | присвоение | `m r0 2.5` | $r0 = 2{,}5$ |
| $+$ | сложение | `+ r0 r1` | $r0 = r0 + r1$ |
| $-$ | вычитание | `- r1 r2` | $r1 = r1 - r2$ |
| $*$ | умножение | `* r2 r0` | $r2 = r2 \cdot r0$ |
| $/$ | деление | `/ r3 r2` | $r3 = r3 / r2$ |
| $^$ | степень | `^ r4 r1` | $r4 = r4^{\,r1}$ |

### Команды работы с массивом памяти

| Команда | Действие | Пример | Результат |
|---------|----------|--------|-----------|
| `mw i ист` | запись | `mw 0 r1` | $w[0] = r1$ |
| `mr rj i` | чтение | `mr r4 2` | $r4 = w[2]$ |
| `+w i rj` | сложение | `+w 0 r4` | $w[0] = w[0] + r4$ |
| `-w i rj` | вычитание | `-w 1 r0` | $w[1] = w[1] - r0$ |
| `*w i rj` | умножение | `*w 2 r0` | $w[2] = w[2] \cdot r0$ |
| `/w i rj` | деление | `/w 3 r2` | $w[3] = w[3] / r2$ |

### Пример фрагмента программы

Инициализация и вычисление $f(x)=x^3-2x-5$ при $x=w[0]$:

```
mw 0 r1
mr r4 0
m r0 r4
m r7 3
^ r0 r7
m r5 2
* r5 r4
- r0 r5
m r7 5
- r0 r7
mw 1 r0
```

---

## Ответы (для проверки)

| № | Ответ |
|---|-------|
| 1 | $36$ |
| 2 | $93$ |
| 3 | $84$ |
| 4 | Коля — $1004$, Лена — $321$ |
| 5а | $9$ |
| 5б | $28261$ |
| 6 | Ошибки: `mx = 0` → `mx = -1`; `if abs(a - b) >= mx` → `if abs(a - b) > mx`; `mx = a - b` → `mx = abs(a - b)` |
| 7 | `YES`, если $y \ge x^2-4$, $y \le 5-x^2$, $y \le 3-2x$ |
| 8 | Корень $\approx 2{,}095$; программа использует массив $w[]$ для $x$, $f(x)$, $f'(x)$ и нового приближения |
