# Вариант 3 — письменный экзамен

**Специальность:** «Проектирование, разработка и управление сложными информационными системами»  
**Время:** 180 минут  
**Задания 1–6** — на бумаге, **задания 7–8** — на компьютере (Python).

---

## Задание 1 (10 баллов)

Найдите значение выражения:

$$\left(\frac{480\cdot 10^{-1}}{2}\right):\left(\frac{\left(25\cdot2+5:0.05\right):\frac{1}{6}}{\left(2\frac{1}{2}:\frac{1}{4}-0.5:\frac{1}{4}\right):0.04}-1\right)^{-1}$$

---

## Задание 2 (10 баллов)

Найдите количество целых значений $x$, при которых выполняется неравенство:

$$\sqrt{1764(x+1)^2-1}>\sqrt{1764-(x-1)^2}$$

---

## Задание 3 (15 баллов)

Найдите радиус окружности, описанной около треугольника со сторонами 13, 14 и 15.

---

## Задание 4 (15 баллов)

Артём и Соня три раза обменивались наклейками. Каждый раз одна четвёртая часть наклеек Артёма менялась на одну треть наклеек Сони. Сколько наклеек было у Артёма и Сони изначально, если после второго обмена у Артёма осталось 720 наклеек, а после третьего обмена у Сони осталось 490?

---

## Задание 5 (10 баллов)

В выражении $358_{x54}+1x0_{100}$ ($x$ — неизвестная цифра):

а) определите наибольшее значение $x$, при котором значение выражения кратно 99;

б) для найденного $x$ найдите частное от деления значения выражения на 99.

*Ответ запишите в десятичной системе счисления.*

---

## Задание 6 (10 баллов)

Дима написал программу, которая по натуральному числу $n$ должна выводить **наименьшую сумму двух соседних цифр** этого числа (цифры считаются соседними, если стоят рядом в десятичной записи). В коде есть ошибки (рис. а).

а) Укажите ошибки.  
б) Запишите исправленные строки.

```python
n = 85627413
mn = 10
while n >= 10:
    a = n % 10
    n = n // 10
    b = n % 10
    if a + b <= mn:
        mn = a
print(mn)
```

---

## Задание 7 (15 баллов)

На координатной плоскости заданы графики функций $y=-x^2+1$, $y=3-x^2$ и $y=x-2$. Закрашенной отмечена область, ограниченная этими графиками (рис. б).

Напишите программу, которая по координатам точки $(x,y)$ определяет, принадлежит ли она закрашенной области.

**Формат ввода:** строка из двух вещественных чисел $x$ и $y$.  
**Формат вывода:** `YES`, если точка принадлежит области, иначе `NO`.

| Ввод | Вывод |
|------|-------|
| -1 1 | YES |
| 1.5 1.5 | NO |

**Описание закрашенной области** (для рисунка):

- $y \ge -x^2 + 1$ (выше параболы $y=-x^2+1$);
- $y \le 3 - x^2$ (ниже параболы $y=3-x^2$);
- $y \ge x - 2$ (выше или на прямой $y=x-2$).

---

## Задание 8 (15 баллов)

Разработайте программу для исполнителя «Сириус», которая **методом половинного деления** находит приближённый корень уравнения

$$f(x)=x^3-3x-1=0$$

на отрезке $[1;\,2]$ с точностью $\varepsilon=0{,}001$.

**Вход:**

- $r1$ — левая граница отрезка ($1$);
- $r2$ — правая граница отрезка ($2$);
- $r3$ — требуемая точность $\varepsilon$ ($0{,}001$).

**Выход:** приближённое значение корня в регистре $r5$.

**Требования к решению:**

1. Промежуточные значения (текущие границы отрезка, середина, значения функции) храните в **массиве памяти** $w[0], w[1], \ldots$ — не в стеке и не только в регистрах.
2. На каждой итерации обновляйте как минимум ячейки $w[0]$ (левая граница), $w[1]$ (правая граница), $w[2]$ (середина), $w[3]$ (значение $f$ в середине).
3. Итерации выполняйте, пока $|w[1]-w[0]| > \varepsilon$.

### Регистры и массив памяти

Исполнитель имеет 8 регистров $r0$–$r7$ и **массив памяти** $w[0], w[1], \ldots, w[9]$ (индексация с нуля). Каждая ячейка хранит одно вещественное число.

### Команды работы с регистрами

| Символ | Действие | Пример | Результат |
|--------|----------|--------|-----------|
| $m$ | присвоение | `m r0 2.5` | $r0 = 2{,}5$ |
| $+$ | сложение | `+ r0 r1` | $r0 = r0 + r1$ |
| $-$ | вычитание | `- r1 r2` | $r1 = r1 - r2$ |
| $*$ | умножение | `* r2 r0` | $r2 = r2 \cdot r0$ |
| $/$ | деление | `/ r3 r2` | $r3 = r3 / r2$ |
| $^$ | степень | `^ r4 r1` | $r4 = r4^{\,r1}$ |

### Команды работы с массивом памяти

| Команда | Действие | Пример | Результат |
|---------|----------|--------|-----------|
| `mw i ист` | запись в массив | `mw 0 r1` | $w[0] = r1$ |
| `mw i c` | запись константы | `mw 3 0` | $w[3] = 0$ |
| `mr rj i` | чтение из массива | `mr r4 2` | $r4 = w[2]$ |
| `+w i rj` | сложение с ячейкой | `+w 0 r4` | $w[0] = w[0] + r4$ |
| `-w i rj` | вычитание | `-w 1 r4` | $w[1] = w[1] - r4$ |
| `*w i rj` | умножение | `*w 3 r0` | $w[3] = w[3] \cdot r0$ |
| `/w i rj` | деление | `/w 2 r7` | $w[2] = w[2] / r7$ |

*Здесь $i$ — индекс ячейки ($0$–$9$), $ист$ — регистр или числовая константа, $c$ — константа.*

### Пример фрагмента программы

Инициализация границ и вычисление середины отрезка:

```
mw 0 r1
mw 1 r2
mr r0 0
+ r0 r1
mr r4 1
+ r0 r4
m r7 2
/ r0 r7
mw 2 r0
```

---

## Ответы (для проверки)

| № | Ответ |
|---|-------|
| 1 | $84$ |
| 2 | $83$ |
| 3 | $\dfrac{65}{8}$ (или $8{,}125$) |
| 4 | Артём — $924$, Соня — $261$ |
| 5а | $7$ |
| 5б | $17374$ |
| 6 | Ошибки: `mn = 10` → `mn = 18`; `if a + b <= mn` → `if a + b < mn`; `mn = a` → `mn = a + b` |
| 7 | `YES`, если $y \ge -x^2+1$, $y \le 3-x^2$, $y \ge x-2$ |
| 8 | Корень $\approx 1{,}879$; программа использует массив $w[]$ для границ и значений $f$ |
